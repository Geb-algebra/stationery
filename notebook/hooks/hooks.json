{
  "SessionStart": [
    {
      "type": "command",
      "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/init.sh",
      "timeout": 5000,
      "description": "Initialize notebook files if they don't exist"
    }
  ]
}
